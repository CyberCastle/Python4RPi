version: 2.1

jobs:
  build:
    machine:
      image: ubuntu-2004:current
    resource_class: arm.large
    steps:
      - checkout

      - run:
          name: Build Python
          command: |
            chmod u+x install-pyton.sh
            ./install-pyton.sh

      - store_artifacts:
          path: /artifact
